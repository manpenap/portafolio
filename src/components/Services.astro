---
// src/components/Services.astro
---

<section id="services" class="py-16 px-4 bg-white dark:bg-gray-900">
  <div class="container mx-auto">
    <div class="text-center mb-12">
      <h2 class="text-4xl font-bold text-gray-900 dark:text-white mb-4">
        🛠️ ¿Qué puedo hacer por tu negocio?
      </h2>
      <p class="text-xl text-gray-600 dark:text-gray-300 max-w-3xl mx-auto">
        Soluciones digitales pensadas para profesionales que necesitan automatizar procesos y mejorar la conversión
      </p>
    </div>

    <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3 max-w-6xl mx-auto">
      <!-- Servicio 1 -->
      <div class="bg-gradient-to-br from-pink-50 to-purple-50 dark:from-gray-800 dark:to-gray-700 p-6 rounded-xl shadow-lg hover:shadow-xl transition-all transform hover:-translate-y-2 opacity-0 translate-y-10" data-animate="fade-up">
        <div class="text-3xl mb-4">📧</div>
        <h3 class="text-xl font-semibold mb-3 text-gray-900 dark:text-white">
          Automatización de Leads
        </h3>
        <p class="text-gray-700 dark:text-gray-300">
          Automatizar la captación y seguimiento de leads (formulario → base de datos → email)
        </p>
      </div>

      <!-- Servicio 2 -->
      <div class="bg-gradient-to-br from-blue-50 to-indigo-50 dark:from-gray-800 dark:to-gray-700 p-6 rounded-xl shadow-lg hover:shadow-xl transition-all transform hover:-translate-y-2 opacity-0 translate-y-10" data-animate="fade-up">
        <div class="text-3xl mb-4">🎯</div>
        <h3 class="text-xl font-semibold mb-3 text-gray-900 dark:text-white">
          Landing Pages Efectivas
        </h3>
        <p class="text-gray-700 dark:text-gray-300">
          Diseñar landing pages que comunican claro, convierten y filtran
        </p>
      </div>

      <!-- Servicio 3 -->
      <div class="bg-gradient-to-br from-green-50 to-emerald-50 dark:from-gray-800 dark:to-gray-700 p-6 rounded-xl shadow-lg hover:shadow-xl transition-all transform hover:-translate-y-2 opacity-0 translate-y-10" data-animate="fade-up">
        <div class="text-3xl mb-4">🔗</div>
        <h3 class="text-xl font-semibold mb-3 text-gray-900 dark:text-white">
          Integración de Herramientas
        </h3>
        <p class="text-gray-700 dark:text-gray-300">
          Integrar herramientas como MailerLite, Notion, APIs de CRM o formularios
        </p>
      </div>

      <!-- Servicio 4 -->
      <div class="bg-gradient-to-br from-orange-50 to-red-50 dark:from-gray-800 dark:to-gray-700 p-6 rounded-xl shadow-lg hover:shadow-xl transition-all transform hover:-translate-y-2 opacity-0 translate-y-10" data-animate="fade-up">
        <div class="text-3xl mb-4">⚙️</div>
        <h3 class="text-xl font-semibold mb-3 text-gray-900 dark:text-white">
          Procesos Digitales
        </h3>
        <p class="text-gray-700 dark:text-gray-300">
          Diseñar procesos digitales para profesionales que venden servicios
        </p>
      </div>

      <!-- Servicio 5 -->
      <div class="bg-gradient-to-br from-purple-50 to-pink-50 dark:from-gray-800 dark:to-gray-700 p-6 rounded-xl shadow-lg hover:shadow-xl transition-all transform hover:-translate-y-2 opacity-0 translate-y-10" data-animate="fade-up">
        <div class="text-3xl mb-4">🚀</div>
        <h3 class="text-xl font-semibold mb-3 text-gray-900 dark:text-white">
          Salto Digital
        </h3>
        <p class="text-gray-700 dark:text-gray-300">
          Ayudar a pequeñas empresas a dar el salto digital sin complicarse
        </p>
      </div>

      <!-- Servicio 6 -->
      <div class="bg-gradient-to-br from-teal-50 to-cyan-50 dark:from-gray-800 dark:to-gray-700 p-6 rounded-xl shadow-lg hover:shadow-xl transition-all transform hover:-translate-y-2 opacity-0 translate-y-10" data-animate="fade-up">
        <div class="text-3xl mb-4">💼</div>
        <h3 class="text-xl font-semibold mb-3 text-gray-900 dark:text-white">
          Consultoría Técnica
        </h3>
        <p class="text-gray-700 dark:text-gray-300">
          Perfil técnico con visión comercial para tu equipo o proyecto
        </p>
      </div>
    </div>
  </div>

  <!-- Script para animación al hacer scroll -->
  <script>
    if (!window.hasRunServicesObserver) {
      window.hasRunServicesObserver = true;

      document.addEventListener("DOMContentLoaded", () => {
        const observer = new IntersectionObserver(
          (entries) => {
            entries.forEach((entry, index) => {
              if (entry.isIntersecting) {
                setTimeout(() => {
                  entry.target.classList.add("animate-fade-in-up");
                }, index * 100);
              }
            });
          },
          { threshold: 0.1 }
        );

        const elements = document.querySelectorAll('#services [data-animate="fade-up"]');
        elements.forEach((el) => observer.observe(el));
      });
    }
  </script>

  <style>
    @keyframes fade-in-up {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .animate-fade-in-up {
      animation: fade-in-up 0.6s ease-out forwards;
    }
  </style>
</section>
